[project]
name = "flowaccount_connector"
authors = [{ name = "K Garden", email = "ai.developer@kgarden.local" }]
description = "Push ERPNext sales documents to FlowAccount via OpenAPI"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<17.0.0"
